{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["cd apps/backend && npm ci"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["cd apps/backend && npx prisma generate && npm run build"]
    }
  },
  "deploy": {
    "startCommand": "cd apps/backend && npx prisma migrate deploy && node dist/app.js"
  }
}
